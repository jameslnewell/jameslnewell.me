import styled from 'styled-components';
import {darken} from 'polished';

const Link = styled.a`
  color: rgb(0, 0, 255);
  transition: color 0.25s;
  &:hover {
    color: ${darken(0.2, 'rgb(0, 0, 255)')};
  }
`;

export default Link;